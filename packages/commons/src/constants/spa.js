// Temporary base path to activate the SPA.
export const SPA_BASE_PATH = '/spa';
// Max navigations allowed in SPA mode. Any further navigation will be a hard nav.
export const MAX_NAVIGATIONS_COUNT = 20;
// CustomEvent for pushing a SPA state
export const EVENT_PUSH_SPA_STATE = 'pushSpaState';
// Query param to enable / disable SPA
export const SPA_MODE = 'spaMode';
// Client error redirect force redirect
export const FORCE_REDIRECT = 'forceRedirect';
// Picture in picture video flag
export const VISIBILITY_SENSOR_SCROLL_THROTTLE = 250;
