import { css } from 'styled-components';
import { DARKER_GREY } from '@univision/fe-commons/dist/utils/styled/constants';

export default {
  wrapper: css`
    background: ${DARKER_GREY};
    display: inline-block;
    padding: 10px 15px;
  `,
};
