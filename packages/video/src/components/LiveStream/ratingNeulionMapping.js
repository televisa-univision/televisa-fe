/**
 * URL relative paths
 * If included in array will trigger the livestream rating validator
 */
const ratingNeulionMapping = {
  '/tudn-livestream-24-7': 'udn',
};

export default ratingNeulionMapping;
