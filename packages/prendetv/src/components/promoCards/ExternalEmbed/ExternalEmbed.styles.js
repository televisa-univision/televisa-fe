/**
 * @module PrendeTV External Embed Promo Card styles
 */
import { css } from 'styled-components';

import { WHITE } from '@univision/fe-utilities/styled/constants';

export default {
  wrapper: css`
    background-color: ${WHITE};
  `,
};
