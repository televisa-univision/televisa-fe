/**
 * @module localization
 */
import LocalizationManager from './LocalizationManager';

/**
 * Localization instance of {@link module:localization~LocalizationManager} with common l10n
 */
export default new LocalizationManager();
