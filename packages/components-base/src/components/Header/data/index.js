import programas from './programas/mapping';

export default {
  // Shows needs legacy configuration because it has
  // custom header that no was migrated 100% to the new navigation
  ...programas,
};
